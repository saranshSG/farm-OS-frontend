import { createMuiTheme } from "@material-ui/core/styles";

const Theme = createMuiTheme({
  palette: {
    primary: {
      main: "#045922",
    },
    secondary: {
      main: "#a4d3e7",
    },
  },
  typography: {
    fontFamily: "Poppins",
  },
});

export default Theme;
